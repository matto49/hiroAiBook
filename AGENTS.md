# AGENTS.md

## 项目用途

这个仓库是一个中文 LaTeX 小册子项目，用于创作一本以《学园偶像大师》中筱泽广为叙事声音和课堂框架的 AI 主题同人/创作小册。

这个项目已经不只是通用 LaTeX 模板。之后处理它时，请把它视为一个写作项目，核心目标有三层：

1. 用 LaTeX 制作一本可读性良好的 JIS B5（182 mm × 257 mm）小册子。
2. 用“筱泽广小课堂”的形式，把 AI 主题写得轻盈、有角色感、有一点初星学园式日常。
3. 在仓库里保留足够的《学园偶像大师》世界观和筱泽广人设资料，保证后续写作口吻一致。

## 重要文件

- `main.tex`：LaTeX 主入口和整体样式定义。
- `chapters/`：正文稿件目录。这里的 `.tex` 文件是小册子正文。
- `figures/`：封面和后续插图资源。
- `docs/reference/gakumas-hiro/`：《学园偶像大师》世界观、筱泽广人设、关系、台词风格、场景提示和来源链接的写作资料包。
- `README.md`：基础编译说明。

## 编译方式

正文包含中文和日文，请使用 XeLaTeX。

```bash
latexmk -xelatex main.tex
```

手动编译备用：

```bash
xelatex main.tex
xelatex main.tex
```

`main.pdf`、`main.aux`、`main.log`、`main.toc`、`main.xdv` 等 LaTeX 生成物不是源文件，不要把它们当成需要手工维护的内容。

## 写作方向

编辑正文时，请尽量保持以下方向：

- 主体语言使用中文。
- 可以少量保留有角色味道的日文词，例如 `プロデューサー`、`初星学園`、`だ、ね`。
- 筱泽广的声音应当安静、观察细致、身体纤弱、头脑清晰，并且会被困难和“不适合自己的事”吸引。
- Producer 与广的关系应写成“被理解后的严厉”，而不是空洞的恶意或单纯宠溺。
- 不要把广简化成单纯病弱少女，也不要写成单纯受虐笑话。她的魅力来自弱身体、清晰头脑和主动挑战失败三者的组合。

写筱泽广式对白时，优先使用：

- 短句；
- 轻柔停顿，如 `...` 或 `……`；
- 平静直接的表达；
- 先观察事实，再给出稍微异常但自洽的情绪结论；
- 少用感叹号。

示例风格：

```text
做不到。
嗯，很好。
这里就是今天的课题。
```

## 资料使用

写作或改写筱泽广相关场景前，先参考：

```text
docs/reference/gakumas-hiro/README.md
```

它主要用于：

- 世界观锚点；
- 角色语气；
- 人物关系；
- 场景灵感；
- 角色扮演一致性。

不要把大段受版权保护的歌词、游戏脚本或原作台词直接复制进正文。可以使用短锚点、概括和原创仿写。

## LaTeX 注意事项

当前文档是 JIS B5（182 mm × 257 mm）`book`，主要使用 `ctex`、`geometry`、`hyperref`、`titlesec`、`fancyhdr` 和 `tikz`。

已知结构问题：

- 部分自定义章节标题页可能没有推进 LaTeX 的正常 chapter 计数。
- 多个章节里重复使用 `ref1`、`ref2` 这类 bibliography label，可能触发重复标签警告。
- `fancyhdr` 可能提示 `\headheight` 太小。
- 手动加入目录的星号章节可能触发重复 anchor 警告。

做版式清理时，应优先修复 LaTeX 结构原因，而不是只隐藏警告。

## 编辑规则

- 正文改动要聚焦，不要顺手做无关格式重排。
- 优先编辑 `.tex` 和 `.md` 源文件，不要手工编辑生成的 PDF。
- 角色资料放在 `docs/`；可出版/可编译的正文放在 `chapters/`。
- 如果补充新的考据或角色资料，请在资料文档中附来源链接。
- 如果添加图片，请放进 `figures/`，并在 LaTeX 中使用相对路径引用。
- 只要对 `.tex` 做了有意义的改动，完成后运行 `latexmk -xelatex main.tex`，并报告是否还有影响输出质量的警告。

## 当前项目意图

接下来较自然的工作顺序是：

1. 稳定 LaTeX 结构，处理现有编译警告。
2. 把筱泽广写作资料转化成正文里的角色声音。
3. 扩写 AI 主题章节，同时保留“初星学园小课堂”的角色扮演框架。
4. 在视觉资源准备好后，替换或补充漫画/插图占位。
